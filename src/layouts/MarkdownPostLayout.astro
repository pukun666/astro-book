---
import BaseLayouter from './BaseLayout.astro';
const { frontmatter } = Astro.props;
---
<BaseLayouter pageTitle={frontmatter.title}>
    <h1>{frontmatter.title}</h1>
    <p>Written by {frontmatter.author}</p>
    <slot />
</BaseLayouter>
